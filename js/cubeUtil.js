function createRubixCube(cubes,width,height,depth){
    var geom = new THREE.BoxGeometry(width,height,depth,15,15,15);
    var mat = createMat();
    
    for()
}

function createMat(){
    var mat = [];
}